import 'package:flutter/material.dart';

class AppColors {
  // Nền tổng thể
  static const Color backgroundDark = Color(0xFF004643);    // Nền tối
  static const Color backgroundLight = Color(0xFFABD1C6);   // Nền sáng
  static const Color surface = Color(0xFFE8E4E6);            // Nền card

  // Chữ
  static const Color textDark = Color(0xFF001E1D);           // Headline (tối)
  static const Color textLight = Color(0xFFFFFFFE);          // Headline (sáng)
  static const Color textMuted = Color(0xFFABD1C6);          // Đoạn văn nhẹ
  static const Color subHeadline = Color(0xFF0F3433);        // Sub-headline

  // Accent & CTA
  static const Color primary = Color(0xFFF9BC60);            // Button, highlight
  static const Color tertiary = Color(0xFFE16162);           // Màu nhấn phụ
}
